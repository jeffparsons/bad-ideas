[workspace]
resolver = "2"
members = ["crates/*"]

# Make sure the "release" numbers we report are actually optimized. Applies to
# every implementation in the workspace so the comparison stays apples-to-apples.
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
