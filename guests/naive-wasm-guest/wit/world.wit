package bad-ideas:naive@0.1.0;

interface sim {
  record entity {
    px: f32,
    py: f32,
    vx: f32,
    vy: f32,
  }

  // Advance one entity by one integration step of `dt` seconds.
  advance: func(e: entity, dt: f32) -> entity;
}

world guest {
  export sim;
}
